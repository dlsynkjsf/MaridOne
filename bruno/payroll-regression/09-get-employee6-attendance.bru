meta {
  name: 09 Get Employee 6 Attendance
  type: http
  seq: 9
}

get {
  url: {{baseUrl}}/api/logs/attendance
  body: none
  auth: bearer
}

auth:bearer {
  token: {{hr_access_token}}
}

params:query {
  empId: {{employee6_id}}
  page: 0
  size: 50
}

tests {
  test("returns 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
